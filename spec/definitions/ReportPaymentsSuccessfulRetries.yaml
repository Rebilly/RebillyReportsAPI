type: object
properties:
  data:
    type: array
    items:
      type: object
      description: Contains a report of payments successful retries throughput (percentage)
      readOnly: true
      properties:
        declineCode:
          type: string
          description: Decline reason code
        declineMessage:
          type: string
          description: Decline reason message
        successfullPercentage:
          type: integer
          description: Successfull percentage
