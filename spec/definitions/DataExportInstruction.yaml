type: object
description: Defines the parameters of the requested data export
required:
  - name
  - format
  - resource
properties:
  name:
    description: Export name
    type: string
  createdById:
    description: ID of the user who created the export
    type: string
    readOnly: true
  createdByName:
    description: Name of the user who created the export
    type: string
    readOnly: true
  createdByEmail:
    description: Email of the user who created the export
    type: string
    readOnly: true
  resource:
    description: 'The type of resource being exported (eg., transactions).'
    type: string
    enum:
      - transactions
  format:
    description: Export result format
    type: string
    enum:
      - csv
  arguments:
    description: >-
      Export request arguments that can be passed to filter and/or sort the
      result set
    type: object
    properties:
      filter:
        description: >-
          The collection items filter requires a special format. Use "," for
          multiple allowed values. Use ";" for multiple fields. See the filter
          guide for more options and examples about this format.
        type: string
      sort:
        type: array
        description: >-
          The collection items sort field and order (prefix with "-" for
          descending sort).
        items:
          type: string
      q:
        type: string
        description: The partial search of the text fields.
      criteria:
        $ref: '#/definitions/Condition'
  dateRange:
    $ref: '#/definitions/DateRange'
  emailNotification:
    description: List of emails to be notified when export is completed
    type: array
    items:
      type: string
      format: email
  exportFields:
    description: List of fields to be exported
    type: array
    items:
      type: string
