description: |
  The definition of the recurring schedule.
  If omitted, then the export happens immediately and is not recurring.
type: object
discriminator: method
properties:
  method:
    description: The interval of recurring time
    type: string
    enum:
      - "daily"
      - "weekly"
      - "monthly"
required:
  - method
