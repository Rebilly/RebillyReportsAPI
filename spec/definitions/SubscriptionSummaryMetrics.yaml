type: object
properties:
  currency:
    description: Currency (three letter ISO 4217 alpha code) (eg USD, EUR)
    readOnly: true
    type: string
  invoicedAmount:
    description: "Total amount invoiced. Formatted according to the currency, example: $23.02"
    readOnly: true
    type: string
  collectedAmount:
    description: "Total amount paid. Formatted according to the currency, example: $15.85"
    readOnly: true
    type: string
  invoiceCount:
    description: Number of issued, past due, or paid invoices
    readOnly: true
    type: number
    format: int
