description: Custom Date Range
allOf:
  -
    $ref: "#/definitions/DateRange"
  -
    type: "object"
    properties:
      duration:
        description: Duration
        type: integer
      unit:
        description: Unit
        type: string
        enum:
          - day
          - week
          - month
          - year
