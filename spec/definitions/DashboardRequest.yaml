type: object
properties:
  segments:
    type: object
    description: Filtered segments for each metric
    properties:
      segmentName:
        type: object
        description: Custom segment name to be used in the response with segment settings
        properties:
          metric:
            description: Type of data and grid of the metric.
            enum:
              - approvedThroughput
              - salesAllCount
              - salesValue
              - refundsValue
          criteria:
            $ref: '#/definitions/Condition'
