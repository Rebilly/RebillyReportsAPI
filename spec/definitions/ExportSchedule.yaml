type: object
required:
  - dataExportInstruction
  - recurringInstruction
  - scheduledStartTime
properties:
  id:
    description: The export identifier string
    readOnly: true
    allOf:
      - $ref: "#/definitions/ResourceId"
  dataExportInstruction:
    allOf:
      - $ref: "#/definitions/DataExportInstruction"
  recurringInstruction:
    type: string
    description: RRule RFC 5545 formatted string used for recurring exports
  scheduledStartTime:
    type: string
    format: date-time
    description: The date and time for when to begin recurring scheduled exports
  nextOccurrenceTime:
    type: string
    format: date-time
    description: The date and time of the next scheduled Export
    readOnly: true
  createdTime:
    description: Export schedule created time
    allOf:
      - $ref: "#/definitions/ServerTimestamp"
  updatedTime:
    description: Export schedule updated time
    allOf:
      - $ref: "#/definitions/ServerTimestamp"
  _links:
    type: array
    description: The links related to resource
    readOnly: true
    minItems: 1
    maxItems: 2
    items:
      - $ref: "#/definitions/SelfLink"
      - $ref: "#/definitions/CreatedByLink"
