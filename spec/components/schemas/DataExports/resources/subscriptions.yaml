description: Subscriptions resource type to export
allOf:
  - $ref: "#/components/schemas/DataExport"
  - type: "object"
    properties:
      dateRange:
        type: object
        properties:
          field:
            default: 'createdTime'
            example: 'createdTime'
