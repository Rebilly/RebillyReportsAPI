allOf:
  -
    $ref: "#/components/schemas/Error"
  -
    type: object
    properties:
      invalidFields:
        type: object
        description: An object keyed by the invalid property field names.  Dot notation is used for nested object property names.
