{
  "name": "rebilly-openapi-reports-spec",
  "version": "0.0.1",
  "dependencies": {
    "json-merge-patch": "^0.2.3",
    "json-pointer": "^0.6.0",
    "@redocly/openapi-cli": "^0.8.0"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi validate",
    "gh-pages": "echo \"Not implemented\""
  }
}